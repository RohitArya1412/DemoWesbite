import { ImpurepipesPipe } from './impurepipes.pipe';

describe('ImpurepipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ImpurepipesPipe();
    expect(pipe).toBeTruthy();
  });
});
