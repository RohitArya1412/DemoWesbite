import { MooksearchPipe } from './mooksearch.pipe';

describe('MooksearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MooksearchPipe();
    expect(pipe).toBeTruthy();
  });
});
